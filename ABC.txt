import ttkbootstrap as ttk
from ttkbootstrap.constants import *

my_w = ttk.Window()
my_w.geometry("500x500")
m1 = ttk.Meter(my_w, amountused=65, metersize=400, meterthickness=50, bootstyle='success')

m1.grid(row=1, column=1, pads=10, pady=10)

my_w.mainloop()
